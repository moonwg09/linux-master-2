
# [리눅스 마스터 2급] 2장: 파일 시스템 권한 및 그룹 설정

## 1. 파일 유형 (File Types) 및 `ls` 명령어
파일의 상세 정보를 확인하는 `ls -l` 명령어의 출력 결과 중 **첫 번째 글자**는 파일의 유형을 나타냅니다.

| 기호 | 파일 유형 | 설명 |
|:---:|---|---|
| `-` | 일반 파일 (Regular File) | 텍스트, 실행 파일, 이미지 등 일반적인 파일 |
| `d` | 디렉터리 (Directory) | 파일들을 담고 있는 디렉터리 |
| `l` | 심볼릭 링크 (Symbolic Link) | 다른 파일을 가리키는 링크 (Windows의 바로가기) |
| `b` | 블록 디바이스 (Block Device) | 하드디스크, CD/DVD 등 블록 단위로 데이터 전송 |
| `c` | 문자 디바이스 (Character Device) | 마우스, 키보드 등 문자 단위로 데이터 전송 |
| `s` | 소켓 (Socket) | 네트워크 통신에 사용되는 파일 |
| `p` | 파이프 (Pipe) | 프로세스 간 통신(IPC)에 사용되는 파일 |

## `ls -l` 출력 결과 예시
터미널에서 `ls -l`을 입력했을 때 나오는 전형적인 파일과 디렉터리의 출력 예시입니다.

> **예시 1 (일반 파일):** `-rw-r--r--. 1 user1 user1  1024  4월 19 17:02 test.txt`
> **예시 2 (디렉터리):** `drwxr-xr-x. 2 root  root   4096  4월 19 17:00 my_dir`

---

## 각 필드별 상세 분석
위의 '예시 1'(`-rw-r--r--. 1 user1 user1 1024 4월 19 17:02 test.txt`)을 기준으로, 왼쪽부터 오른쪽 순서대로 필드를 쪼개서 설명합니다.

| 순서 | 필드 값 | 항목명 | 상세 설명 |
|:---:|:---:|---|---|
| **1** | `-` | **파일 유형** | 가장 첫 번째 글자입니다. `-`는 일반 파일, `d`는 디렉터리, `l`은 심볼릭 링크를 의미합니다. |
| **2** | `rw-r--r--` | **파일 권한**<br>(퍼미션) | 소유자(u), 그룹(g), 기타(o) 사용자별 읽기(`r`), 쓰기(`w`), 실행(`x`) 권한을 나타내는 9자리 문자입니다. |
| **3** | `.` | **확장 속성** | (시험 외 참고) SELinux 보안 문맥이나 ACL(접근 제어 목록)이 설정되어 있음을 의미합니다. 없는 경우도 있습니다. |
| **4** | `1` | **하드 링크 수** | 이 파일과 연결된 하드 링크의 개수입니다. (디렉터리의 경우 기본값이 보통 2부터 시작합니다.) |
| **5** | `user1` | **소유자명** | 파일을 소유하고 있는 사용자(Owner)의 계정 이름입니다. |
| **6** | `user1` | **소유 그룹명** | 파일이 속해 있는 그룹(Group)의 이름입니다. |
| **7** | `1024` | **파일 크기** | 파일의 용량입니다. **단위는 바이트(Byte)**입니다. (`ls -lh` 옵션을 쓰면 KB, MB 단위로 보기 쉽게 출력됩니다.) |
| **8** | `4월 19 17:02` | **최종 수정일** | 파일의 내용이 **마지막으로 변경(수정)된 날짜와 시간**입니다. (mtime) |
| **9** | `test.txt` | **파일/디렉터리명** | 파일이나 디렉터리의 실제 이름입니다. 숨김 파일은 이름 앞에 점(`.`)이 붙어 있습니다. |

---

## 3. 시험 대비 핵심 체크포인트 ★★★

* **첫 번째 10글자의 구조:** 파일 유형(1자리) + 소유자 권한(3자리) + 그룹 권한(3자리) + 기타 권한(3자리)의 조합임을 명심하세요.
* **파일 크기의 단위:** `ls -l`의 기본 출력 단위는 무조건 **Byte(바이트)**입니다. 시험에서 KB나 블록 단위라고 속이는 보기를 주의하세요.
* **디렉터리의 크기:** 빈 디렉터리라도 구조 정보를 담기 위해 기본적으로 **4096 Byte(4KB)** 등의 크기를 가집니다. (파일이 들어있다고 해서 디렉터리 자체의 크기 숫자가 파일 용량만큼 계속 커지는 것은 아닙니다.)

---

## 2. 파일 권한 설정: `chmod` (Change Mode)
파일이나 디렉터리의 접근 권한(Read, Write, Execute)을 변경합니다.

### 2.1 숫자(8진수) 모드 (절대 모드)
가장 많이 쓰이며 시험에도 자주 나오는 방식입니다.
* **읽기(r)** = 4
* **쓰기(w)** = 2
* **실행(x)** = 1

> **예시:** `chmod 755 test.txt`
> * 소유자(User): 7 (4+2+1 = rwx)
> * 그룹(Group): 5 (4+1 = r-x)
> * 기타(Others): 5 (4+1 = r-x)

### 2.2 문자(심볼릭) 모드 (상대 모드)
* **대상:** `u`(User), `g`(Group), `o`(Others), `a`(All)
* **연산자:** `+`(부여), `-`(제거), `=`(지정)
* **권한:** `r`, `w`, `x`

> **예시:** `chmod u+x,go-w test.txt`
> (소유자에게 실행 권한 추가, 그룹과 기타 사용자에게서 쓰기 권한 제거)

---

## 3. 소유권 및 그룹 변경: `chown`, `chgrp`

### `chown` (Change Owner)
파일이나 디렉터리의 **소유자와 소유 그룹**을 변경합니다.
* `chown user1 file` : 소유자를 user1로 변경
* `chown user1:group1 file` : 소유자와 그룹을 동시에 변경 (콜론 `:` 대신 마침표 `.` 사용 가능)
* **💡 주요 옵션:** `-R` (하위 디렉터리와 파일까지 모두 변경 - **시험 단골 출제**)

### `chgrp` (Change Group)
파일이나 디렉터리의 **소유 그룹**만 변경합니다.
* `chgrp group1 file`

---

## 4. 기본 권한 마스크: `umask`
새로 생성되는 파일이나 디렉터리의 **기본 권한을 결정**하는 값입니다. (최대 권한에서 umask 값을 뺌)

* **디렉터리 최대 권한:** 777 (`rwxrwxrwx`)
* **파일 최대 권한:** 666 (`rw-rw-rw-`) -> *파일은 생성 시 실행(x) 권한을 가질 수 없음*

> **계산 예시 (umask가 022일 때):**
> * **새 디렉터리:** 777 - 022 = **755** (`rwxr-xr-x`)
> * **새 파일:** 666 - 022 = **644** (`rw-r--r--`)

---

## 5. 특수 권한 (Special Permissions) ★★★
시험에 무조건 나오는 핵심 파트입니다. 일반 권한 앞에 4자리 숫자로 표현됩니다.

| 특수 권한 | 8진수 값 | 기호 | 설명 | 대표적인 예시 |
|---|:---:|:---:|---|---|
| **SetUID** | `4000` | `s` | 실행하는 동안 **파일 소유자의 권한**을 가짐 (User의 x 자리에 s) | `/usr/bin/passwd` |
| **SetGID** | `2000` | `s` | 실행하는 동안 **파일 소유 그룹의 권한**을 가짐 (Group의 x 자리에 s) | |
| **Sticky Bit** | `1000` | `t` | 누구나 쓰기 가능하지만, **삭제는 소유자와 관리자만** 가능 (Others의 x 자리에 t) | `/tmp` 디렉터리 |

> **표기 예시:**
> * SetUID가 설정된 권한: `rwsr-xr-x` (숫자로 `4755`)
> * Sticky Bit가 설정된 권한: `rwxrwxrwt` (숫자로 `1777`)
> * *주의:* 원래 실행 권한(x)이 없는데 특수 권한이 부여되면 대문자(`S`, `T`)로 표시됩니다.

---

## 6. 디스크 할당량: `Quota`
사용자나 그룹이 사용할 수 있는 디스크 공간(용량)이나 파일 수(i-node)를 제한하는 기능입니다.

* **Soft Limit:** 설정 용량을 넘을 수 있지만, 유예 기간(Grace Period) 내에 정리해야 함.
* **Hard Limit:** 절대 넘을 수 없는 최대 한계치.

### 주요 Quota 명령어
1. `quotacheck`: 파일 시스템을 검사하고 쿼터 파일(`aquota.user`, `aquota.group`)을 생성/업데이트
2. `edquota`: 에디터(vi)를 열어 사용자/그룹의 쿼터를 직접 설정 및 편집
3. `quotaon` / `quotaoff`: 쿼터 기능을 켜거나 끔
4. `repquota`: 파일 시스템의 쿼터 사용 현황을 요약하여 출력 (Report)
5. `quota`: 특정 사용자의 현재 쿼터 설정과 사용량을 확인

# [리눅스 마스터 2급] 2장: 파일 시스템 관리 및 구조

## 1. 파일 시스템의 핵심 구조
리눅스 파일 시스템(특히 ext 계열)을 구성하는 논리적인 주요 요소입니다.

| 항목 | 설명 | 주요 특징 |
|:---:|---|---|
| **inode** | 파일의 상세 정보(메타데이터) 저장 | 파일 크기, 권한, 소유자, 데이터 블록 위치 등 (**파일명은 제외**) |
| **Superblock** | 파일 시스템의 전체적인 상태 정보 | 전체 블록 수, 사용 가능한 블록 수, inode 크기 등 |
| **Data Block** | 실제 데이터가 저장되는 공간 | 파일의 실제 내용이 기록됨 |

---

## 2. 주요 파일 시스템 종류
시험에서는 각 파일 시스템의 특징과 최대 용량을 묻는 경우가 많습니다.

### 2.1 ext4 (Fourth Extended File System)
* 현재 리눅스에서 가장 널리 쓰이는 표준 파일 시스템입니다.
* **최대 지원:** 단일 파일 16TB, 전체 1EB(엑사바이트).
* **특징:** **저널링(Journaling)** 기능을 지원하여 파일 시스템 손상을 방지합니다.

### 2.2 XFS
* SGI에서 개발한 고성능 64비트 파일 시스템입니다.
* 대용량 파일 및 고성능 처리에 유리하여 최신 RHEL/CentOS의 기본 파일 시스템으로 쓰입니다.

### 2.3 NFS (Network File System)
* 네트워크를 통해 다른 호스트의 파일 시스템을 자신의 로컬 디렉터리처럼 사용하는 네트워크 파일 시스템입니다.

### 💡 저널링(Journaling)이란?
데이터를 실제 파일 시스템에 쓰기 전에 로그(저널)에 먼저 기록하는 기술입니다. 시스템이 갑자기 꺼졌을 때 재부팅 시 데이터 복구 시간을 획기적으로 단축해 줍니다.

---

## 3. 디스크 관리 및 복구 명령어

### `fdisk` (Fixed Disk)
* 디스크의 **파티션을 생성, 삭제, 수정**하는 명령어입니다.
* MBR(Master Boot Record) 파티션 테이블 방식을 주로 사용합니다. (2TB 이하 권장)

### `fsck` (File System Check)
* 리눅스 파일 시스템의 **무결성을 점검하고 수리**하는 명령어입니다.
* 주의: 마운트된 상태에서 실행하면 데이터가 손상될 수 있으므로 반드시 **언마운트(umount)** 후 실행해야 합니다.

---

## 4. 마운트(Mount) 및 설정

### `mount` / `umount`
* 특정 장치(하드디스크, USB 등)를 리눅스 디렉터리 구조에 연결하거나 해제합니다.
* 예시: `mount /dev/sdb1 /mnt/data`

### `/etc/fstab`
* 리눅스 부팅 시 파일 시스템을 **자동으로 마운트**하기 위한 설정 파일입니다.
* **fstab의 6개 필드 구성 (시험 단골):**
  1. `Device`: 장치명 또는 UUID
  2. `Mount Point`: 마운트될 위치
  3. `File System Type`: ext4, xfs, nfs 등
  4. `Options`: defaults, ro, rw 등
  5. `Dump`: 0(덤프 안함), 1(덤프 가능)
  6. `Pass`: fsck 점검 순서 (0: 안함, 1: 루트, 2: 기타)

---

## 5. 전체 흐름 요약
새 디스크를 추가하여 사용할 때의 논리적 순서입니다.

1. **`fdisk`**: 디스크 파티션 나누기
2. **`mkfs`**: 파일 시스템 생성(포맷)
3. **`mount`**: 디렉터리에 연결하여 사용
4. **`/etc/fstab` 등록**: 부팅 시 자동 마운트 설정

리눅스 마스터 2급 합격을 위한 세 번째 정리 노트입니다!

이번 파트는 쉘(Shell)과 커널(Kernel)의 관계, 그리고 사용자 환경 설정과 직결되는 내용이라 기출문제에 아주 자주 등장합니다. 특히 chsh와 usermod 명령어의 옵션을 구분하는 것이 핵심입니다.

바로 복사해서 사용하실 수 있도록 마크다운(.md) 파일 형식으로 정리해 드립니다.


# [리눅스 마스터 2급] 2장: 쉘(Shell)의 개념 및 종류

## 1. 커널(Kernel)과 쉘(Shell)의 역할
운영체제를 구성하는 두 가지 핵심 요소입니다. 시험에서는 이 둘의 역할을 묻는 개념 문제가 자주 출제됩니다.

| 구분 | 역할 | 비유 |
|:---:|---|---|
| **커널 (Kernel)** | 하드웨어를 직접 제어하고 메모리, 프로세스를 관리하는 **운영체제의 핵심(Core)** | 식당의 '주방장' (실제 요리를 하는 사람) |
| **쉘 (Shell)** | 사용자가 입력한 명령어를 해석하여 커널에 전달하는 **명령어 해석기** | 식당의 '웨이터' (주문을 받아 주방에 전달) |

---

## 2. 주요 쉘(Shell)의 종류 및 특징
다양한 쉘이 존재하며, 각 쉘의 특징과 발전 배경을 묻는 문제가 나옵니다.

* **bash (Bourne Again Shell)**
  * 현재 리눅스의 **표준(기본) 쉘**입니다.
  * GNU 프로젝트에 의해 개발되었으며, Bourne shell(sh)과 호환성이 뛰어납니다.
  * 명령어 자동 완성(Tab), 히스토리(History), 에일리어스(Alias) 기능을 지원합니다.
* **csh (C Shell)**
  * C 언어와 유사한 구문(Syntax)을 가지는 쉘입니다.
  * 히스토리, 에일리어스, 작업 제어 등의 기능을 초기에 도입하여 인기를 끌었습니다.
* **zsh (Z Shell)**
  * bash, csh, ksh 등의 강력한 기능들을 모두 포함한 **확장형 쉘**입니다.
  * 최근 macOS의 기본 쉘로 채택될 만큼 강력한 플러그인과 테마 기능을 제공합니다.

---

## 3. 로그인 쉘(Login Shell) vs 서브 쉘(Sub Shell)

### 로그인 쉘 (Login Shell)
* 사용자가 ID와 비밀번호를 입력하고 **시스템에 처음 접속할 때** 실행되는 쉘입니다.
* 전역 환경 설정(`/etc/profile`)과 사용자 개인 환경 설정(`~/.bash_profile` 등)을 모두 읽어옵니다.

### 서브 쉘 (Sub Shell / Non-login Shell)
* 이미 로그인된 상태에서 **추가로 실행한 쉘**입니다. (예: 터미널 창을 하나 더 열거나, 쉘 스크립트를 실행할 때)
* 전역 설정은 건너뛰고, 사용자 전용 설정 파일(`~/.bashrc`)만 읽어옵니다.

---

## 4. 쉘 관련 주요 설정 파일 및 명령어 ★★★
자신의 쉘을 확인하거나 변경할 때 사용하는 명령어들입니다. (옵션 암기 필수)

### `/etc/shells`
* 현재 시스템에서 **사용 가능한(설치된) 쉘의 목록**이 저장된 파일입니다.
* `cat /etc/shells` 명령어로 확인할 수 있습니다.

### `chsh` (Change Shell)
* 로그인하는 사용자의 **기본 쉘을 변경**하는 전용 명령어입니다.
* **시험 단골 옵션:**
  * `chsh -s [변경할 쉘 경로] [사용자명]` : 지정한 쉘로 변경합니다.
  * `chsh -l` : `/etc/shells` 파일에 명시된 사용 가능한 쉘 목록을 출력합니다.
> **예시:** `chsh -s /bin/zsh user1` (user1의 기본 쉘을 zsh로 변경)

### `usermod` (User Modify)
* 사용자 계정의 다양한 정보(홈 디렉터리, 그룹, 만료일 등)를 변경하는 다목적 명령어입니다.
* **쉘 변경 옵션 (`-s`):**
> **예시:** `usermod -s /bin/bash user1` (user1의 기본 쉘을 bash로 변경)

💡 **핵심 요약:** 쉘만 전용으로 바꿀 때는 `chsh`, 사용자 계정 정보를 전반적으로 수정하면서 쉘도 바꿀 때는 `usermod`를 사용합니다. 두 명령어 모두 쉘 변경 옵션은 `-s`로 동일합니다!


# [리눅스 마스터 2급] 2장: 쉘 환경 설정 및 변수 관리

## 1. 쉘 변수 vs 환경 변수
쉘에서 사용되는 변수는 적용 범위에 따라 두 가지로 나뉩니다.

| 구분 | 설명 | 확인 명령어 |
|---|---|---|
| **지역 변수 (쉘 변수)** | 현재 사용 중인 쉘(Shell)에서만 유효한 변수입니다. 서브 쉘이나 다른 프로세스로 전달되지 않습니다. | `set` (모든 변수 출력) |
| **환경 변수 (전역 변수)** | 현재 쉘뿐만 아니라 서브 쉘이나 실행되는 모든 프로그램에 전달되어 영향을 미치는 변수입니다. | `env`, `printenv` |

---

## 2. 주요 명령어: `export`, `env`, `set`

### `export`
지역 변수(쉘 변수)를 **환경 변수로 승격(Export)** 시켜 서브 쉘에서도 사용할 수 있게 만듭니다. 시험에 가장 자주 나오는 명령어입니다.
* `export 변수명=값` : 환경 변수 생성 및 값 할당
* `export 변수명` : 기존의 쉘 변수를 환경 변수로 변경
* `export -n 변수명` : 환경 변수를 다시 쉘 변수로 내림(해제)

### `env` / `set`
* `env` : 현재 설정된 **환경 변수** 목록만 출력합니다.
* `set` : 환경 변수뿐만 아니라 쉘 변수, 쉘 함수 등 **모든 변수** 목록을 출력합니다.

---

## 3. 주요 환경 변수 (`PATH` 및 기타)
환경 변수는 대문자로 표기하며, 변수값을 사용할 때는 앞에 `$`를 붙입니다. (예: `echo $PATH`)

* **`PATH`**: 실행 파일을 찾을 경로들의 목록입니다. 콜론(`:`)으로 구분됩니다. (예: `/usr/bin:/bin:/usr/sbin`)
* **`HOME`**: 현재 사용자의 홈 디렉터리 경로입니다.
* **`USER`**: 현재 로그인한 사용자의 이름입니다.
* **`SHELL`**: 현재 사용 중인 로그인 쉘의 경로입니다.
* **`PWD`**: 현재 작업 중인 디렉터리 경로입니다. (Print Working Directory)

> **`PATH` 변수에 경로 추가하기:**
> `export PATH=$PATH:/new/path` (기존 PATH 값 뒤에 `/new/path`를 추가)

---

## 4. 쉘 설정 파일 (Profile & rc 파일) ★★★
시스템 전역 설정인지, 사용자 개별 설정인지 구분하는 것이 핵심입니다.

### 전역 설정 (모든 사용자에게 적용)
* **`/etc/profile`**: 로그인할 때 모든 사용자에게 적용되는 전역 환경 변수 및 시작 프로그램을 설정합니다.
* **`/etc/bashrc`**: 모든 사용자에게 적용되는 전역 alias 및 쉘 함수를 설정합니다.

### 지역 설정 (특정 사용자에게만 적용)
사용자의 홈 디렉터리(`~`) 안에 숨김 파일(`.`) 형태로 존재합니다.
* **`~/.bash_profile`** (또는 `~/.profile`): 개별 사용자의 환경 변수(`PATH` 등)를 설정합니다. 로그인 시 한 번만 읽힙니다.
* **`~/.bashrc`**: 개별 사용자의 **alias** 및 쉘 함수를 설정합니다. 서브 쉘을 열 때마다 읽힙니다.
* **`~/.bash_logout`**: 사용자가 로그아웃할 때 실행할 명령어를 정의합니다. (예: 임시 파일 삭제)

---

## 5. 히스토리(History) 관리
사용자가 이전에 입력했던 명령어들의 기록을 관리합니다.

* **`history`**: 지금까지 입력한 명령어 목록을 번호와 함께 출력합니다.
* **`~/.bash_history`**: 히스토리 내역이 실제 텍스트 형태로 저장되는 파일입니다. (로그아웃 시 메모리에서 파일로 저장됨)
* **`HISTSIZE`**: 메모리에 기억할 히스토리의 최대 개수를 설정하는 환경 변수입니다. (기본값 보통 1000)
* **`HISTFILESIZE`**: `~/.bash_history` 파일에 저장할 최대 줄(명령어) 수를 설정하는 환경 변수입니다.

> **히스토리 단축키 (시험 대비):**
> * `!!` : 바로 직전에 실행한 명령어 재실행
> * `!n` : n번째 명령어 재실행 (예: `!50`)
> * `!문자열` : 지정한 문자열로 시작하는 가장 최근 명령어 재실행

---

## 6. 에일리어스 (Alias)
길고 복잡한 명령어를 짧고 기억하기 쉬운 별명으로 만들어 줍니다.

* **`alias`**: 현재 설정된 모든 alias 목록을 출력합니다.
* **`alias 별명='명령어'`**: 새로운 alias를 생성합니다. (예: `alias ll='ls -l'`)
* **`unalias 별명`**: 설정된 alias를 삭제합니다.

💡 **주의:** 커맨드라인에서 `alias` 명령어로 설정한 값은 재부팅하면 사라집니다. 영구적으로 적용하려면 `~/.bashrc` 파일에 기록해야 합니다.

# [리눅스 마스터 2급] 2장: 프로세스(Process)의 개념 및 유형

## 1. 프로세스 기본 개념
프로세스는 하드디스크에 저장된 프로그램이 메모리에 적재되어 **현재 실행 중인 상태**를 말합니다.

* **PID (Process ID)**
  * 리눅스에서 실행되는 모든 프로세스에 부여되는 **고유한 식별 번호**입니다.
* **`init` 프로세스 (PID 1)**
  * 시스템 부팅 시 커널에 의해 **가장 먼저 실행되는 프로세스**입니다.
  * 시스템의 모든 프로세스의 직/간접적인 '부모 프로세스' 역할을 합니다. (최신 리눅스에서는 `systemd`가 이 역할을 대체하고 있으나, 시험에서는 주로 `init`을 묻습니다.)

---

## 2. 프로세스 생성 방식: `fork()` vs `exec()` ★
시험에서 두 가지 생성 방식의 차이를 묻는 문제가 단골로 출제됩니다.

| 구분 | 설명 | PID 변화 |
|:---:|---|---|
| **`fork()`** | 현재 실행 중인 프로세스(부모)를 **그대로 복사**하여 새로운 프로세스(자식)를 생성합니다. | **새로운 PID**를 부여받음 |
| **`exec()`** | 원래의 프로세스 메모리 공간에 **새로운 프로그램을 덮어씌워** 실행합니다. | **기존 PID를 그대로 유지** |

---

## 3. 포그라운드(Foreground)와 백그라운드(Background)

* **포그라운드 프로세스 (Foreground Process)**
  * 사용자가 입력한 명령이 실행되어 결과가 출력될 때까지 **터미널을 차지하는 프로세스**입니다.
  * 실행 중에는 프롬프트가 떨어지지 않아 다른 명령을 입력할 수 없습니다.
* **백그라운드 프로세스 (Background Process)**
  * 명령어 실행 시 뒤에 **`&`** 기호를 붙여 실행합니다. (예: `find / -name test &`)
  * 프로세스가 실행되는 동안에도 터미널을 자유롭게 사용할 수 있습니다.

---

## 4. 데몬 (Daemon) 프로세스 ★★★
시스템이 부팅될 때 백그라운드에서 실행되어, 사용자의 요청(서비스)을 처리하기 위해 항상 대기하는 서버 프로세스입니다. 보통 이름 끝에 `d`가 붙습니다. (예: `httpd`, `sshd`)

### 데몬의 2가지 동작 방식 (매우 중요)
1. **Standalone (독립 실행형) 방식**
   * 데몬이 **항상 메모리에 상주**하며 직접 요청을 처리합니다.
   * **장점:** 응답 속도가 매우 빠릅니다. (웹 서버, 메일 서버 등 접속이 잦은 서비스에 적합)
   * **단점:** 시스템 리소스(메모리)를 항상 차지합니다.
2. **`inetd` / `xinetd` (슈퍼 데몬) 방식**
   * 평소에는 슈퍼 데몬(`inetd` 또는 `xinetd`)만 메모리에 상주하며 대기합니다.
   * 요청이 들어오면 슈퍼 데몬이 해당 서비스 데몬을 실행시켜 연결해 주고, 처리가 끝나면 메모리에서 해제합니다.
   * **장점:** 메모리 등 시스템 자원을 효율적으로 사용합니다. (telnet, ftp 등 접속 빈도가 낮은 서비스에 적합)
   * **단점:** Standalone 방식에 비해 응답 속도가 약간 느립니다.

---

## 5. 시그널 (Signal)과 주요 번호
프로세스에게 특정 이벤트를 알리거나 제어(종료, 재시작 등)하기 위해 보내는 **소프트웨어 인터럽트**입니다.

### `kill -l`
* 시스템에서 사용할 수 있는 **모든 시그널의 종류와 번호 목록을 출력**하는 명령어입니다.

### 주요 시그널 번호 (반드시 암기)
| 번호 | 시그널 이름 | 단축키 | 설명 |
|:---:|:---:|:---:|---|
| **1** | **SIGHUP** | - | 프로세스 재시작 (설정 파일 변경 후 적용할 때 주로 사용) |
| **2** | **SIGINT** | `Ctrl + C` | 실행 중인 프로세스를 인터럽트하여 **정상 종료** |
| **9** | **SIGKILL** | - | 프로세스 **강제 종료** (무조건 종료시키며, 무시할 수 없음) |
| **15** | **SIGTERM** | - | 프로세스 정상 종료 (`kill` 명령어의 **기본(default) 시그널**) |
| **20** | **SIGTSTP** | `Ctrl + Z` | 실행 중인 작업을 **일시 정지(Suspend)**하여 백그라운드로 전환 준비 |

# [리눅스 마스터 2급] 2장: 프로세스 유틸리티 및 제어

## 1. 프로세스 상태 모니터링
가장 기본적이며 시험 출제 비중이 매우 높은 명령어들입니다.

### `ps` (Process Status)
현재 실행 중인 프로세스의 상태를 출력합니다. (표기법이 System V 계열과 BSD 계열로 나뉩니다.)
* **`ps -ef` (System V 계열)**: 시스템의 모든 프로세스를 전체 포맷(Full)으로 출력. (PID, PPID 확인 가능)
* **`ps aux` (BSD 계열)**: 시스템의 모든 프로세스를 소유자(User) 중심의 상세 포맷으로 출력. (CPU, 메모리 사용률 등 확인 가능)

### `pstree`
프로세스의 부모-자식 관계를 **트리(Tree) 형태**로 시각화하여 계층 구조를 직관적으로 파악하게 해줍니다.

### `top`
리눅스 시스템의 전반적인 상태(CPU, 메모리 사용량, 프로세스 목록)를 **실시간으로 갱신**하여 보여줍니다. (Windows의 작업 관리자와 동일한 역할)

---

## 2. 백그라운드 및 작업(Job) 제어
터미널을 효율적으로 사용하기 위한 백그라운드 전환 및 유지 명령어입니다.

* **`jobs`**: 현재 쉘에서 실행 중인 **백그라운드 작업 목록과 번호(Job ID)**를 출력합니다.
* **`nohup` (No Hang Up)**: 로그아웃하거나 터미널 창을 닫아도 **프로세스가 종료되지 않고 계속 백그라운드에서 실행**되도록 합니다.
  * 실행 시 발생하는 표준 출력(화면 출력)은 자동으로 `nohup.out` 파일에 저장됩니다.
  * *예시:* `nohup ./backup.sh &` (백그라운드 실행 기호 `&`와 항상 짝꿍으로 쓰입니다.)

---

## 3. 프로세스 강제 종료
앞서 배운 '시그널(Signal)'을 프로세스에 보내어 동작을 제어합니다.

* **`kill`**: **PID(프로세스 ID)**를 지정하여 특정 프로세스를 종료합니다. (예: `kill -9 1234`)
* **`killall`**: PID가 아닌 **프로세스 이름**을 지정하여, 같은 이름을 가진 모든 프로세스를 한 번에 종료합니다. (예: `killall httpd`)

---

## 4. 프로세스 우선순위 관리 ★★★
프로세스가 CPU 자원을 할당받는 우선순위를 조정합니다. **NI(Nice) 값**으로 제어하며, 범위는 `-20`부터 `19`까지입니다. **숫자가 작을수록 우선순위가 높습니다(더 빨리 실행됨).**

| 구분 | `nice` | `renice` |
|:---:|---|---|
| **역할** | 프로세스를 **처음 실행할 때** 우선순위를 설정 | **이미 실행 중인** 프로세스의 우선순위를 변경 |
| **대상** | 프로세스(명령어) 이름 | **PID (프로세스 ID)** |
| **옵션** | `-n [값]` (생략 시 기본값은 +10) | `-p [PID]` (또는 PID 번호만 바로 입력) |
| **예시** | `nice -n -5 bash` | `renice -10 -p 1234` |

> **💡 시험 핵심 포인트:**
> 일반 사용자는 NI 값을 높이는 것(우선순위 낮추기)만 가능합니다. **NI 값을 낮추어 우선순위를 높이는 작업은 `root` 관리자만 가능**합니다!

---

## 5. 정기적인 작업 스케줄링: `crontab` ★★★
정해진 시간이나 날짜에 반복적으로 특정 명령이나 스크립트를 실행하도록 설정합니다. 설정 파일의 6가지 필드 순서는 무조건 암기해야 합니다.

### Crontab 필드 구조 (순서 암기: 분-시-일-월-요-명)
| 1 | 2 | 3 | 4 | 5 | 6 |
|:---:|:---:|:---:|:---:|:---:|---|
| **분** (0~59) | **시** (0~23) | **일** (1~31) | **월** (1~12) | **요일** (0~7) | **실행할 명령어** |

* *요일: 0과 7은 일요일, 1=월, 2=화, ..., 6=토*
* *`*` (별표): 모든 시간/날짜를 의미 (예: 분 자리에 `*`가 있으면 '매분'이라는 뜻)*

> **기출문제 예시 해석:** `30 4 * * 1 /bin/backup.sh`
> 👉 **매주 월요일(1) 새벽 4시 30분**에 `/bin/backup.sh` 스크립트를 실행하라.

# [리눅스 마스터 2급] 4장: 에디터(Editor)의 종류 및 특징

## 1. 에디터의 핵심 개념
편집기의 동작 방식과 데이터를 처리하는 기본 개념입니다.

* **버퍼 (Buffer)**
  * 에디터로 파일을 열었을 때, 원본 파일이 바로 수정되는 것이 아니라 파일 내용이 임시로 복사되어 올라가는 **메모리 공간**입니다.
  * 작업을 마치고 '저장' 명령을 내려야만 버퍼의 내용이 실제 하드디스크의 파일에 덮어씌워집니다.
* **모드형 편집기 (Modal Editor)**
  * 상태(모드)에 따라 키보드 입력의 결과가 달라지는 방식입니다. (예: `vi`, `vim`)
  * '입력 모드'에서는 글자가 타이핑되고, '명령 모드'에서는 같은 키를 눌러도 복사/붙여넣기 등의 커맨드로 작동합니다.
* **비모드형 편집기 (Non-Modal Editor)**
  * 윈도우의 메모장처럼 키를 누르면 무조건 글자가 입력되고, 명령어는 단축키(Ctrl, Alt 등)를 조합하여 사용하는 방식입니다. (예: `emacs`, `nano`)

---

## 2. 주요 리눅스 에디터 총정리 ★★★
시험에서 **에디터 이름, 특징, 창시자**를 연결하는 문제가 거의 매번 출제됩니다.

| 에디터 | 창시자 (개발자) | 주요 특징 |
|:---:|:---:|---|
| **vi** | 빌 조이 (Bill Joy) | 유닉스/리눅스의 표준 텍스트 에디터. **모드형 편집기**의 대표 주자입니다. |
| **vim** | **브람 무레나르** (Bram Moolenaar) | vi와 호환되면서 문법 강조(Syntax Highlighting), 다중 실행 취소 등 기능을 대폭 강화한 편집기(Vi IMproved)입니다. |
| **emacs** | **리처드 스톨만** (Richard Stallman) | 강력한 매크로 기능을 가진 대표적인 **비모드형 편집기**입니다. LISP 언어 기반으로 환경 설정이 자유롭고 확장성이 매우 뛰어납니다. |
| **pico** | 워싱턴 대학 (Aboil Kassam 등) | 유닉스 기반 메일 프로그램인 Pine에 통합된 에디터입니다. 화면 하단에 단축키 도움말이 나와 초보자가 쓰기 쉽습니다. |
| **nano** | GNU 프로젝트 | pico의 복제(Clone) 버전으로 만들어진 무료 에디터입니다. 최근 우분투 등 여러 배포판에서 초보자를 위한 기본 에디터로 많이 채택됩니다. |

---

## 3. GUI(그래픽 환경) 기반 에디터
X-Window 시스템(그래픽 환경)에서 마우스와 함께 사용하는 에디터들입니다.

* **gedit**: **GNOME (그놈)** 데스크톱 환경의 기본 텍스트 에디터입니다. 윈도우의 메모장과 비슷하며 직관적입니다.
* *(참고) KWrite / Kate*: KDE 데스크톱 환경의 기본 텍스트 에디터입니다.

> **💡 시험장 가기 전 필수 암기 포인트**
> * "브람 무레나르가 만들었으며 문법 강조 기능이 있는 것" 👉 **vim**
> * "리처드 스톨만이 만들었고 강력한 매크로를 지원하는 비모드형 편집기" 👉 **emacs**
> * "입력 모드와 명령 모드가 구분되어 있는 편집기" 👉 **vi, vim**

# [리눅스 마스터 2급] 4장: 에디터 활용 (vi, emacs, pico)

## 1. vi 에디터의 3가지 동작 모드 ★★★
`vi`는 상태에 따라 키보드 입력 결과가 달라지는 '모드형 편집기'입니다. 이 3가지 모드의 상호 전환을 묻는 문제가 자주 출제됩니다.

| 모드 (Mode) | 설명 | 특징 및 전환 방법 |
|:---:|---|---|
| **명령 모드**<br>(Command) | `vi` 실행 시의 **기본 상태**. 글자 입력은 안 되며, 커서 이동, 복사, 삭제, 붙여넣기 등을 수행합니다. | 입력/EX 모드에서 **`ESC`**를 누르면 무조건 명령 모드로 돌아옵니다. |
| **입력 모드**<br>(Insert/Edit) | 텍스트를 **실제로 타이핑**하여 문서 내용을 작성하는 상태입니다. | 명령 모드에서 **`i`, `a`, `o`** 등의 키를 누르면 진입합니다. |
| **EX 명령 모드**<br>(Last-line) | 화면 맨 아랫줄에 명령어를 입력하여 저장, 종료, 검색, 치환 등을 수행합니다. | 명령 모드에서 **`:` (콜론)**, `/`, `?` 등을 누르면 진입합니다. |

---

## 2. vi 에디터: 필수 단축키 (명령 모드)
명령 모드에서 사용하는 키보드 단축키입니다. 이 중 `dd`와 `yy`는 시험 단골입니다.

* **삭제 (잘라내기)**
  * **`dd`**: 커서가 있는 **현재 줄(1줄)을 삭제**합니다. (내부적으로 잘라내기로 동작하여 메모리에 저장됨)
  * `3dd`: 커서가 있는 줄부터 아래로 3줄을 삭제합니다.
  * `x`: 커서가 위치한 글자 1개를 삭제합니다. (Delete 키와 동일)
* **복사 및 붙여넣기**
  * **`yy` (Yank)**: 커서가 있는 **현재 줄(1줄)을 복사**합니다.
  * `3yy`: 커서가 있는 줄부터 아래로 3줄을 복사합니다.
  * `p` (Paste): 복사하거나 잘라낸(삭제한) 내용을 커서 아래(또는 오른쪽)에 붙여넣습니다.

---

## 3. vi 에디터: 저장 및 종료 (EX 명령 모드) ★★★
명령 모드에서 `:`(콜론)을 누른 뒤 화면 맨 아래에 입력하는 명령어들입니다.

| 명령어 | 기능 설명 | 기출 핵심 포인트 |
|:---:|---|---|
| **`:w`** | 저장하기 (Write) | 파일 이름 지정 가능 (예: `:w newfile.txt`) |
| **`:q`** | 종료하기 (Quit) | 내용이 수정된 상태라면 경고가 뜨며 종료되지 않음 |
| **`:wq`** | **저장하고 종료하기** | 문서 수정 후 가장 많이 사용하는 명령어 |
| **`:q!`** | **저장하지 않고 강제 종료** | 수정된 내용을 무시하고 그냥 나갈 때 사용 (`!`는 강제를 의미) |

> **💡 시험 대비 팁:**
> 명령 모드 상태에서 대문자 `ZZ`를 누르면 `:wq` (저장 후 종료)와 완벽하게 동일하게 동작합니다. 이 부분도 기출에 자주 등장합니다.

---

## 4. 기타 주요 에디터 활용 요약
vi 외의 에디터들은 복잡한 사용법보다는 특징적인 단축키 형태를 주로 묻습니다.

### emacs (이맥스)
* 리처드 스톨만이 개발한 강력한 편집기입니다.
* 비모드형 편집기이므로, 명령어 실행을 위해 **`Ctrl` 키(화면 기호: `C-`)**와 **`Alt/Meta` 키(화면 기호: `M-`)**의 조합을 매우 많이 사용합니다.
* *예시:* `C-x C-s` (저장), `C-x C-c` (종료)

### pico (피코)
* 윈도우의 메모장처럼 직관적이고 다루기 쉬운 비모드형 편집기입니다.
* 화면 하단에 사용할 수 있는 단축키 메뉴가 항상 표시됩니다.
* 단축키는 주로 **`Ctrl` 키(화면 기호: `^`)**를 사용합니다.
* *예시:* `^O` (저장), `^X` (종료)
