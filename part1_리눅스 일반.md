# 🐧 리눅스 마스터 대비: 파트 1. 리눅스의 이해 요약

리눅스 마스터 1과목 '리눅스의 이해' 섹션에서 반드시 암기해야 할 핵심 키워드 정리입니다.

---

## 1. 리눅스의 탄생과 핵심 인물
* **리누스 토발즈(Linus Torvalds)**
    * 1991년 헬싱키 대학 재학 중 **커널(Kernel) 0.01** 버전을 최초 개발.
    * 미닉스(Minix) 운영체제를 참고하여 유닉스(UNIX) 호환 운영체제로 개발함.
* **리처드 스톨먼(Richard Stallman)**
    * **GNU 프로젝트**의 창시자이며 자유 소프트웨어 재단(FSF) 설립자.
    * "소프트웨어는 공유되어야 한다"는 철학을 전파.

---

## 2. 철학과 라이선스 (GNU, GPL, 오픈소스)

### 🧩 GNU (GNU's Not Unix)
* **의미:** 유닉스와 호환되지만, 유닉스가 아닌 자유 소프트웨어 운영체제를 만드는 프로젝트.
* **특징:** 컴파일러(GCC), 셸(Bash), 에디터(Emacs) 등 운영체제에 필요한 도구들을 개발함.

### 📜 GPL (General Public License)
* **정의:** 자유 소프트웨어 재단(FSF)에서 만든 소프트웨어 라이선스.
* **4가지 자유:** 실행, 복제, 수정, 배포의 자유.
* **핵심 원칙 (Copyleft):** GPL 소스를 사용하여 수정·재배포할 경우, **반드시 수정된 소스 코드도 공개해야 함.**

### 🔓 오픈소스 (Open Source)
* 소스 코드를 공개하여 누구나 자유롭게 개량하고 배포할 수 있는 모델.
* 단, 모든 오픈소스 라이선스가 GPL처럼 소스 공개를 강제하는 것은 아님 (BSD, Apache 등 존재).

---

## 3. 리눅스의 기술적 특징

### 🧠 커널 (Kernel)
* 운영체제의 핵심부로 **하드웨어 제어, 메모리 관리, 프로세스 관리** 등을 담당함.
* 사용자와 하드웨어 사이의 중재자 역할을 수행함.

### 🌐 POSIX (Portable Operating System Interface)
* **개념:** IEEE가 제정한 유닉스 기반 운영체제 간의 **상호 호환성** 표준 규격.
* **중요성:** 리눅스가 POSIX 표준을 따르기 때문에 유닉스용 애플리케이션을 리눅스로 쉽게 이식할 수 있음.

### 👥 멀티유저 & 멀티태스킹
* **멀티유저(Multi-User):** 여러 사용자가 동시에 접속하여 독립된 환경에서 작업 가능.
* **멀티태스킹(Multi-Tasking):** 여러 개의 프로세스를 동시에 실행(시분할 처리) 가능.

---

## 4. 리눅스 배포판 (Distribution)
리눅스 커널에 GNU 소프트웨어와 관리 도구를 합쳐 설치하기 쉽게 만든 패키지.

| 계열 | 주요 배포판 | 패키지 관리 도구 | 특징 |
| :--- | :--- | :--- | :--- |
| **Debian** | Ubuntu, Debian, Kali | `apt`, `dpkg` | 사용자 편의성 및 커뮤니티 중심 |
| **Red Hat** | RHEL, CentOS, Fedora | `yum`, `dnf`, `rpm` | 기업용 시장 강세, 안정성 중시 |
| **Slackware** | OpenSUSE | `zypper` | 유닉스적인 전통 중시 |

---

## 💡 시험 직전 체크리스트 (암기 팁)
- [ ] **GPL**은 소스 공개가 의무이다. (O)
- [ ] **리누스 토발즈**는 커널을 개발했고, **리처드 스톨먼**은 철학을 세웠다.
- [ ] **POSIX**는 '호환성'을 위한 표준 규격이다.
- [ ] 리눅스는 유닉스와 구조적으로 유사하지만 소스코드가 전혀 다른 **독립적 OS**이다.
  

# 🛠️ 리눅스 마스터: 리눅스 설치 및 설정 핵심 정리

## 1. 리눅스 배포판 및 설치 유형
### 📦 주요 배포판 계열
* **Red Hat 계열:** RHEL, Fedora, CentOS, Rocky Linux (패키지 관리: `RPM`, `YUM/DNF`)
* **Debian 계열:** Ubuntu, Debian, Kali (패키지 관리: `dpkg`, `APT`)
* **Slackware 계열:** OpenSUSE (패키지 관리: `zypper`)

### 🛠️ 설치 유형 (데스크톱 vs 서버)
* **데스크톱 설치:** X-Window(GUI) 환경을 포함하며 사무용, 개발용 도구가 함께 설치됨.
* **서버 설치:** CLI(Command Line Interface) 기반의 텍스트 모드 위주. 보안과 성능을 위해 필요한 서비스(Apache, MySQL 등)만 최소 설치함.

---

## 2. 하드웨어 호환성 및 사원(Resource) 체크
### 💻 핵심 하드웨어 점검
* **하드웨어 호환성:** 리눅스는 다양한 하드웨어를 지원하지만, 최신 그래픽카드나 특정 무선 랜카드는 커널 지원 여부를 확인해야 함. (`/proc` 디렉토리에서 확인 가능)
* **CPU:** `/proc/cpuinfo` 명령어로 정보 확인. 아키텍처(x86, x86_64, ARM 등) 확인 필수.
* **메모리(RAM):** 최소 사양 이상 권장. 부족 시 **Swap 파티션**을 활용하여 보완. (`free` 명령어 사용)

---

## 3. 파티션(Partition) 설정
> **시험 단골 주제!** 파티션 구성 방식과 주요 디렉토리의 용도를 숙지하세요.

* **기본 파티션 구성:** `/` (루트), `/boot`, `/home`, `/var`, `swap` 등으로 분할 권장.
* **Swap 파티션:** 물리적 메모리(RAM)가 부족할 때 사용하는 가상 메모리 공간. 보통 **RAM 크기의 1~2배**로 설정.
* **LVM (Logical Volume Manager):** 여러 개의 하드디스크를 하나의 논리적인 볼륨으로 묶어 관리하며, 용량 확장이 유연함.
* **LVM (Logical Volume Manager):** 여러 개의 하드디스크를 하나의 논리적인 볼륨으로 묶어 관리하며, 용량 확장이 유연함.
---

## 4. 백업 (Backup)
* **개념:** 데이터 손실에 대비하여 다른 저장 매체에 복사본을 만드는 것.
* **전략:**
    * **전체 백업 (Full):** 데이터 전체를 복사.
    * **증분 백업 (Incremental):** 마지막 백업 이후 변경된 데이터만 복사.
    * **차등 백업 (Differential):** 전체 백업 이후 변경된 모든 데이터를 복사.

---

## 5. 네트워크 설정 (Network)
### 🌐 네트워크 구성 요소
* **IP 주소:** 네트워크상에서 컴퓨터를 식별하는 고유 주소. (IPv4, IPv6)
* **서브넷 마스크 (Subnet Mask):** IP 주소에서 네트워크 부분과 호스트 부분을 구분하는 기준.
* **게이트웨이 (Gateway):** 외부 네트워크로 나가는 통로(중계 지점).
* **DNS 서버 (Domain Name System):** 도메인 이름(google.com)을 IP 주소(8.8.8.8)로 변환해주는 서버. `/etc/resolv.conf` 파일에서 설정.

### 📝 주요 설정 파일 (CentOS/RedHat 기준)
* `/etc/sysconfig/network-scripts/ifcfg-eth0`: 인터페이스 상세 설정
* `/etc/sysconfig/network`: 호스트네임 및 게이트웨이 설정
* `/etc/resolv.conf`: DNS 서버 정보

---

## 💡 시험 합격 팁
1.  **Swap 파티션**의 정의와 적정 크기 설정법을 기억하세요.
2.  **LVM**과 일반 파티션의 차이점을 묻는 문제가 자주 나옵니다.
3.  네트워크 설정 파일들의 **경로와 파일명**을 정확히 외워두세요.

# 💾 리눅스 마스터: 파티션 및 파일 시스템 핵심 정리

---

## 1. 파티션의 종류와 구조
> 💡 **MBR(Master Boot Record) 방식 기준의 파티션 제한을 이해하는 것이 핵심!**

* **주 파티션 (Primary Partition):** 부팅이 가능한 파티션으로, 하나의 디스크에 최대 **4개**까지만 생성 가능합니다.
* **확장 파티션 (Extended Partition):** 4개 이상의 파티션이 필요할 때 사용합니다. 주 파티션 중 하나를 할당하여 만들며, **데이터 저장 공간이 아닌 논리 파티션을 담는 그릇** 역할만 합니다.
* **논리 파티션 (Logical Partition):** 확장 파티션 안에 생성되는 파티션입니다. 개수 제한이 사실상 없어 여러 개를 만들 수 있습니다.

---

## 2. 장치 식별 및 확인
* **/dev/sd[a-z]:** 리눅스에서 디스크를 인식하는 명칭입니다. (예: 첫 번째 디스크는 `/dev/sda`, 두 번째는 `/dev/sdb`) 뒤에 숫자가 붙으면 파티션 번호입니다. (예: `/dev/sda1`)
* **/proc/partitions:** 현재 시스템의 커널이 인식하고 있는 파티션의 정보를 담고 있는 가상 파일입니다.
* **fdisk:** 리눅스의 전통적인 파티션 관리 도구입니다. (MBR 방식 위주, 2TB 이하)
* **parted:** 2TB 이상의 대용량 디스크(GPT 방식)를 지원하는 고급 파티션 관리 도구입니다.

---

## 3. 파일 시스템과 마운트
### 📂 파일 시스템 타입 (Filesystem)
| 타입 | 특징 |
| :--- | :--- |
| **ext4** | 리눅스의 표준 파일 시스템. 저널링 기능을 지원하며 안정성이 높음. |
| **XFS** | 고성능 저널링 파일 시스템. 대용량 파일 처리에 유리하며 RHEL 7버전부터 기본임. |
| **Swap** | RAM이 부족할 때 디스크 일부를 메모리처럼 사용하는 가상 메모리 영역. |

### 🔗 마운트 (Mount)
* **정의:** 물리적인 장치(파티션)를 리눅스의 특정 디렉토리에 연결하는 과정입니다.
* **명령어:** `mount [장치명] [디렉토리]`, 해제 시에는 `umount`를 사용합니다.

---

## 4. LVM (Logical Volume Manager)
> 💡 **여러 개의 하드디스크를 유연하게 관리하기 위한 기술입니다. 계층 구조를 암기하세요!**

1.  **PV (Physical Volume, 물리 볼륨):** `/dev/sda1` 같은 실제 물리적 파티션입니다.
2.  **VG (Volume Group, 볼륨 그룹):** 여러 개의 PV를 하나로 묶은 커다란 가상 디스크 풀입니다.
3.  **LV (Logical Volume, 논리 볼륨):** VG에서 사용자가 필요한 만큼 떼어내서 만든 최종적인 가상 파티션입니다. (여기에 파일 시스템을 생성함)

---

## 5. RAID (Redundant Array of Independent Disks)
여러 개의 디스크를 묶어 성능이나 안정성을 높이는 기술입니다.

* **RAID 0 (Striping):** 데이터를 나누어 저장. 속도는 빠르나 하나만 고장 나도 전체 데이터 손실.
* **RAID 1 (Mirroring):** 동일한 데이터를 복제. 안정성은 높으나 비용이 많이 듦.
* **RAID 5:** 패리티(Parity) 정보를 분산 저장. 성능과 안정성의 절충안 (최소 디스크 3개 필요).
* **RAID 6:** 패리티를 2개 사용. 디스크 2개가 동시에 고장 나도 복구 가능 (최소 4개 필요).

---

## 🚩 시험 대비 핵심 체크포인트
- [ ] **fdisk vs parted:** 대용량(2TB 이상) 지원 여부에 따른 차이점
- [ ] **LVM 순서:** PV → VG → LV 단계별 용어 구분
- [ ] **Swap 파티션:** 파티션 타입 번호(`82`) 및 용도 확인
- [ ] **장치명:** `/dev/sda`는 디스크 자체, `/dev/sda1`은 첫 번째 파티션

# 💻 리눅스 마스터: 핵심 기본 명령어 및 계정 관리

---

## 1. 명령어 및 경로 관련 (Path & Alias)

### 🔍 위치 및 도움말 찾기
* **which [명령어]:** 특정 명령어가 실행될 때 참조되는 **실행 파일의 절대 경로**를 출력합니다. (PATH 환경 변수에 등록된 경로를 검색)
* **whereis [명령어]:** 실행 파일뿐만 아니라 **소스 코드, 매뉴얼 페이지(man page)** 위치까지 한꺼번에 찾아줍니다.
* **PATH (환경 변수):** 실행 파일을 찾을 경로들의 목록입니다. 각 경로는 콜론(`:`)으로 구분되며, 명령어 입력 시 이 경로들을 순차적으로 뒤져 실행 파일을 실행합니다.
    * 확인: `echo $PATH`

### 🏷️ 별칭 및 매뉴얼
* **alias:** 긴 명령어에 별명을 붙여 짧게 사용할 수 있게 합니다.
    * 설정: `alias ll='ls -l'`
    * 해제: `unalias ll`
* **man [명령어]:** 리눅스 표준 온라인 매뉴얼을 보여줍니다.
* **apropos [키워드]:** 특정 키워드가 포함된 모든 매뉴얼의 제목과 설명을 검색합니다. (명령어 이름이 생각나지 않을 때 유용)

---

## 2. 사용자 계정 관리 (User Management)

### 👤 계정 생성 및 암호
* **useradd [사용자명]:** 새로운 사용자를 생성합니다. (주요 옵션: `-u` UID 지정, `-d` 홈 디렉토리 지정, `-s` 기본 셸 지정)
* **passwd [사용자명]:** 사용자의 비밀번호를 설정하거나 변경합니다.
    * `-l`: 계정 잠금 (Lock)
    * `-u`: 계정 잠금 해제 (Unlock)
    * `-d`: 암호 삭제 (비번 없이 로그인 가능)

### 🔐 보안 및 정책 (/etc/shadow, chage)
* **/etc/shadow:** 사용자 암호가 암호화되어 저장되는 파일입니다. 루트(root)만 읽을 수 있어 `/etc/passwd`보다 보안이 강력합니다.
    * 구조 예시: `ID:PW:최종변경일:최소사용기간:최대사용기간:경고기간:비활성기간:만료일`

* **chage:** 사용자의 암호 만료 및 주기 등 정책을 관리합니다.
    * `-l`: 계정의 상세 암호 정책 확인 (매우 중요!)
    * `-m`: 최소 암호 사용 일수
    * `-M`: 최대 암호 사용 일수
    * `-W`: 경고일수 설정

---

## 🚩 시험 빈출 포인트
| 구분 | 내용 |
| :--- | :--- |
| **which vs whereis** | `which`는 실행 경로만, `whereis`는 매뉴얼과 소스까지 포함. |
| **/etc/shadow** | 실제 비밀번호 해시값이 들어있는 파일 (권한: 400 또는 000). |
| **chage -l** | 특정 사용자의 계정 만료 정보를 조회할 때 가장 많이 쓰이는 옵션. |
| **alias 저장** | 일시적 적용은 터미널에서, 영구 적용은 `~/.bashrc` 파일에 저장. |

---

> 💡 **학습 팁:** `useradd` 실행 시 기본값이 설정되는 파일인 `/etc/default/useradd`와 `/etc/login.defs` 내용도 함께 훑어보시면 100점 방지용 문제까지 대비할 수 있습니다!

# 📂 리눅스 마스터: 디렉터리 및 파일 관리 핵심

---

## 1. 기본 파일 및 디렉터리 조작
* **cd (Change Directory):** 디렉터리 이동.
    * `cd ~`: 홈 디렉터리 이동 / `cd ..`: 상위 디렉터리 이동 / `cd -`: 이전 작업 디렉터리 이동
* **ls -l (List):** 파일 및 디렉터리의 상세 정보(권한, 소유자, 크기, 수정 시간 등)를 리스트 형태로 출력.
* **cp -r (Copy Recursive):** 디렉터리를 복사할 때 사용. 하위 디렉터리와 파일을 모두 포함하여 복사함.
* **rm -rf (Remove Recursive Force):** * `-r`: 디렉터리 삭제 시 사용.
    * `-f`: 삭제 시 확인 메시지를 띄우지 않고 강제로 삭제. (매우 주의해서 사용!)

---

## 2. 검색 및 텍스트 처리 (빈출 ⭐)
* **find -name [파일명]:** 지정한 이름의 파일을 검색.
    * 예: `find /etc -name "*.conf"` (etc 디렉터리에서 확장자가 .conf인 파일 찾기)
* **grep [패턴]:** 파일 내에서 특정 문자열(패턴)을 검색.
    * 예: `grep "root" /etc/passwd`
* **wc -l (Word Count):** 출력 내용의 **줄 수(Line)**를 카운트. 
    * 주로 파이프(`|`)와 조합하여 검색 결과의 개수를 셀 때 사용.
* **diff [파일1] [파일2]:** 두 파일 간의 차이점을 비교하여 출력.

---

## 3. 리다이렉션과 파이프 (Redirection & Pipe)
* **| (Pipe):** 앞 명령어의 **실행 결과**를 뒤 명령어의 **입력**으로 전달.
    * 예: `ls -l /etc | wc -l` (etc 디렉터리 내 파일이 총 몇 개인지 출력)

---

## 4. 정규 표현식 (Regular Expression)
텍스트 검색(grep, vi 등) 시 패턴을 정의하는 기호입니다.

| 기호 | 의미 | 예시 |
| :--- | :--- | :--- |
| **^** | 줄의 **시작**을 의미 | `^apple` : apple로 시작하는 줄 찾기 |
| **$** | 줄의 **끝**을 의미 | `apple$` : apple로 끝나는 줄 찾기 |
| **.** | 임의의 한 문자 | `a.c` : abc, acc, adc 등 매칭 |
| **\*** | 앞의 문자가 0번 이상 반복 | `a*b` : b, ab, aab, aaab 등 매칭 |

---

## 🚩 시험 대비 필승 포인트
1. **find와 grep의 차이:** `find`는 파일 자체를 찾는 것이고, `grep`은 파일 내부의 '문자'를 찾는 것입니다.
2. **wc -l 활용:** 특정 프로세스 개수나 파일 개수를 묻는 문제에서 파이프와 함께 자주 등장합니다.
3. **정규표현식 ^와 $:** * `^$` : 공백 라인(아무것도 없는 줄)을 의미함.
    * `/etc/passwd`에서 특정 계정으로 시작하는 줄을 찾을 때 `grep "^username"` 형태로 자주 쓰임.

---

> 💡 **노션 꿀팁:** `rm -rf` 같은 위험한 명령어는 노션에서 **빨간색 글자**나 **배경색**을 입혀서 주의 표시를 해두면 기억에 오래 남습니다!

# 🌐 리눅스 마스터: 네트워크 진단 및 시스템 관리 명령어

---

## 1. 네트워크 진단 및 정보 조회
네트워크 연결 상태나 DNS 정보를 확인할 때 사용하는 필수 도구들입니다.

* **ping (Packet INternet Groper):** 네트워크 연결 상태를 확인하기 위해 ICMP 패킷을 보냅니다.
    * 상대방 호스트가 살아있는지(Reachability) 확인하는 가장 기본적인 도구입니다.
* **traceroute:** 목적지까지 패킷이 거쳐가는 **경로(Gateway/Router)**를 추적합니다.
    * 어느 지점에서 네트워크 병목이나 장애가 발생하는지 파악할 때 유용합니다.
* **nslookup:** 도메인 네임(DNS) 서버에 질의하여 특정 도메인의 IP 주소를 찾아줍니다. (대화형 모드 지원)
* **dig (Domain Information Groper):** `nslookup`보다 더 상세한 DNS 정보를 출력하는 최신 도구입니다.
    * 리눅스 관리자들이 `nslookup`보다 더 선호하며, 상세한 응답 헤더 정보를 보여줍니다.

---

## 2. 시스템 종료 및 재부팅 (Shutdown & Reboot)
시스템을 안전하게 종료하거나 다시 시작하는 명령어들입니다.

| 명령어 | 설명 | 비고 |
| :--- | :--- | :--- |
| **shutdown -h now** | 시스템을 즉시 **종료(Halt)** 합니다. | `-h`는 Halt의 약자 |
| **shutdown -r now** | 시스템을 즉시 **재부팅(Reboot)** 합니다. | `-r`는 Reboot의 약자 |
| **reboot** | 시스템을 재시작합니다. | `shutdown -r now`와 유사 |
| **halt** | 시스템 운영체제를 중지합니다. | 전원까지 끌지는 설정에 따라 다름 |
| **init 0** | 런레벨 0번(종료 모드)으로 변경하여 시스템 종료 | 런레벨 개념 이해 필수 |
| **init 6** | 런레벨 6번(재부팅 모드)으로 변경하여 시스템 재시작 | |

---

## 3. 기타 관리 명령어
* **date:** 시스템의 현재 날짜와 시간을 출력하거나 설정합니다.
    * 포맷 지정 가능 (예: `date +%Y-%m-%d`)
* **wall (Write All):** 시스템에 접속해 있는 **모든 사용자에게 메시지를 전송**합니다.
    * 주로 시스템 점검이나 종료 직전에 공지할 때 사용합니다.

---

## 🚩 시험 대비 핵심 체크포인트
1. **네트워크 경로 추적:** "목적지까지 가는 경로의 라우터 정보"를 묻는다면 답은 무조건 `traceroute`입니다.
2. **DNS 질의:** 간단한 확인은 `nslookup`, 상세한 정보(응답 시간, 헤더 등)는 `dig`를 사용합니다.
3. **런레벨 번호:** * **0번:** 종료
    * **1번:** 단일 사용자 모드 (복구 모드)
    * **3번:** 텍스트 모드 (멀티 유저)
    * **5번:** 그래픽 모드 (X-Window)
    * **6번:** 재시작
4. **shutdown의 장점:** `reboot`이나 `halt`보다 권장되는 이유는 **종료 예약을 걸 수 있고(`+10`분 뒤 종료 등)**, 사용자들에게 종료 메시지를 보낼 수 있기 때문입니다.

---

> 💡 **학습 팁:** `shutdown -k` 옵션은 실제로 종료하지 않고 "종료될 것"이라는 가짜 경고 메시지만 보냅니다. 이런 특이한 옵션들이 간혹 보너스 문제로 나오니 참고하세요!