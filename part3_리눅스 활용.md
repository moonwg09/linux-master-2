
# [리눅스 마스터 2급] 3장: 리눅스 활용 - X 윈도우(X-Window) 출제빈도 중

## 1. X 윈도우 시스템 개념 및 역사
리눅스의 그래픽 사용자 인터페이스(GUI) 환경을 제공하는 핵심 시스템입니다. 네트워크 기반의 **클라이언트-서버 구조**로 동작하는 것이 가장 큰 특징입니다. (서버가 화면을 뿌려주고, 클라이언트가 입력을 처리합니다.)

* **X11**: X 윈도우 시스템의 11번째 버전을 뜻하며, 현재 리눅스에서 사용하는 X 윈도우의 표준 통신 규약(프로토콜)입니다.

### 주요 프로젝트 (시험 단골)
| 구분 | 설명 |
|:---:|---|
| **XFree86** | 초창기 리눅스에서 가장 널리 쓰였던 오픈소스 X 윈도우 프로젝트입니다. 이후 라이선스(라이선스 변경) 문제로 인해 개발이 위축되었습니다. |
| **X.org** | XFree86의 라이선스 문제 이후 떨어져 나와(Fork) 만들어진 프로젝트로, **현재 리눅스의 표준 X 윈도우 구현체**입니다. |

---

## 2. X 윈도우 프로그래밍 라이브러리
개발자들이 X 윈도우 기반 프로그램을 만들 때 사용하는 C 언어 라이브러리입니다. 둘의 차이를 묻는 문제가 종종 나옵니다.

* **Xlib**: 초창기부터 사용되던 전통적인 X 윈도우 라이브러리입니다. 구조가 크고 다소 무겁습니다.
* **XCB (X C Binding)**: Xlib의 단점을 개선하여 만든 **최신 라이브러리**입니다. 크기가 작고, 비동기적 요청을 지원하여 속도와 성능이 훨씬 뛰어납니다.

---

## 3. X 윈도우 실행 및 환경 변수

### `startx` 명령어
* 텍스트 모드(CLI)에서 X 윈도우를 **수동으로 실행**할 때 사용하는 명령어입니다.
* 내부적으로 `xinit`이라는 명령어를 호출하여 X 서버와 클라이언트 프로그램들을 순차적으로 실행시킵니다.

### `DISPLAY` 환경 변수 ★★★
* 실행된 그래픽 프로그램(X 클라이언트)이 **어떤 모니터(화면)에 출력될지**를 결정하는 매우 중요한 환경 변수입니다.
* **형식:** `export DISPLAY="IP주소:디스플레이번호.스크린번호"`
* *예시:* `export DISPLAY=":0.0"` (앞의 IP가 생략되면 로컬 시스템을 의미하며, 첫 번째 디스플레이의 첫 번째 스크린에 창을 띄우라는 뜻입니다.)

---

## 4. X 윈도우의 3단계 구성 요소 ★★★
화면을 구성하는 요소들의 계층 구조를 묻는 문제가 매번 출제됩니다. 역할별로 정확히 구분해 두세요.

| 구성 요소 | 역할 | 대표적인 종류 |
|:---:|---|---|
| **윈도우 매니저**<br>(Window Manager) | 창의 테두리(Border), 제목 표시줄, 창 이동/크기 조절, 최소화 등 **개별 창의 외양과 동작**을 제어합니다. | Mutter, KWin, Metacity, Enlightenment, Fluxbox 등 |
| **데스크톱 환경**<br>(Desktop Environment) | 윈도우 매니저를 포함하여 파일 관리자, 아이콘, 패널, 바탕화면 등 **전체적인 그래픽 작업 환경**을 세트로 묶어 제공합니다. | **GNOME** (우분투 기본), **KDE** (화려함), **XFCE** (가벼움) |
| **디스플레이 매니저**<br>(Display Manager) | 부팅 시 가장 먼저 나타나는 **그래픽 로그인 화면**을 제공하고, 사용자 인증 및 세션 관리를 담당합니다. | GDM (GNOME용), KDM (KDE용), LightDM 등 |

> **💡 시험장 가기 전 필수 암기 포인트**
> * "창의 테두리를 관리하고 창을 이동시킨다" 👉 **윈도우 매니저**
> * "그래픽 로그인 화면을 제공한다" 👉 **디스플레이 매니저**
> * "GNOME, KDE, XFCE는 무엇의 종류인가?" 👉 **데스크톱 환경(DE)**


# [리눅스 마스터 2급] 3장: 리눅스 활용 - X 윈도우 활용 및 응용 프로그램 출제빈도 하

## 1. X 윈도우 접근 제어 (보안 설정) ★★★
다른 컴퓨터(클라이언트)가 내 리눅스(서버)의 X 윈도우 화면을 띄울 수 있도록 허용하거나 차단하는 방법입니다. 시험에서 `xhost`와 `xauth`의 차이를 묻는 문제가 단골로 출제됩니다.

### `xhost` (호스트 기반 인증)
* **IP 주소나 호스트명**을 기준으로 접근을 제어하는 가장 기본적인 명령어입니다.
* **주요 사용법:**
  * `xhost +` : **모든 클라이언트의 접속을 허용**합니다. (보안상 매우 위험하여 기출문제 정답으로 자주 등장합니다.)
  * `xhost -` : 모든 클라이언트의 접속을 차단합니다.
  * `xhost + 192.168.0.10` : 특정 IP(192.168.0.10)의 접속만 허용합니다.

### `xauth` (쿠키 기반 인증)
* `xhost`보다 보안이 강화된 방식으로, **인증 키(Cookie)**를 가진 사용자만 접속을 허용합니다.
* 생성된 쿠키 정보는 사용자의 홈 디렉터리 내 `.Xauthority`라는 숨김 파일에 암호화되어 저장됩니다.
* **`MIT-MAGIC-COOKIE-1`**: `xauth` 명령어에서 사용하는 **가장 대표적인 인증 키(쿠키) 방식의 이름**입니다. (기출문제에서 "xauth에서 사용하는 쿠키 방식은?"으로 자주 묻습니다.)

---

## 2. 주요 X 윈도우 응용 프로그램
시험에서는 프로그램 이름과 그 용도(특징)를 짝지어 묻는 문제가 매번 출제됩니다. 각 프로그램의 성격을 정확히 키워드로 암기하세요.

| 분류 | 프로그램명 | 핵심 특징 및 기출 키워드 |
|:---:|---|---|
| **오피스** | **LibreOffice**<br>(리브레오피스) | 워드, 엑셀, 파워포인트 등을 대체할 수 있는 대표적인 **오픈소스 오피스 패키지**입니다. (과거의 OpenOffice를 대체하며 Writer, Calc, Impress 등으로 구성됨) |
| **그래픽** | **GIMP**<br>(김프) | 리눅스의 '포토샵'으로 불리는 **비트맵 기반의 강력한 이미지 편집 프로그램**입니다. (GNU Image Manipulation Program) |
| **그래픽** | **ImageMagick**<br>(이미지매직) | 그래픽 화면뿐만 아니라 **터미널(CLI) 명령어를 통해서도 이미지를 편집, 변환**할 수 있는 도구 모음입니다. (`convert`, `display` 등의 명령어를 포함) |
| **멀티미디어** | **Totem**<br>(토템) | **GNOME(그놈) 데스크톱 환경의 기본 미디어 플레이어**입니다. (오디오와 비디오 모두 재생 가능) |
| **개발 도구** | **Eclipse**<br>(이클립스) | 자바(Java)를 비롯해 C, C++, Python 등 다양한 언어를 지원하는 **대표적인 통합 개발 환경(IDE)**입니다. |

> **💡 시험 대비 꿀팁:**
> 응용 프로그램 파트는 깊은 사용법보다는 **"다음 중 성격이 다른 프로그램은?"** (예: 그래픽 프로그램 3개 + 개발 도구 1개 섞어놓기) 또는 **"그놈 환경의 기본 영상 플레이어는?"** 수준으로 얕고 넓게 출제되니, 짝맞추기 위주로 학습하시는 것이 가장 효율적입니다.


# [리눅스 마스터 2급] 3장: 인터넷 활용 - 네트워크 기초 개념 출제 빈도 상 

## 1. 네트워크의 규모에 따른 분류 (LAN vs WAN)
네트워크가 도달하는 물리적인 거리에 따라 구분합니다.

| 구분 | 풀이 | 설명 및 특징 |
|:---:|---|---|
| **LAN** | Local Area Network<br>(근거리 통신망) | 사무실, 학교, 집 등 비교적 **가까운 지역**을 연결하는 네트워크. 초기 구축 비용은 들지만 유지비가 저렴하고 전송 속도가 매우 빠릅니다. |
| **WAN** | Wide Area Network<br>(광대역 통신망) | 국가, 대륙 등 **넓은 지역**을 연결하는 네트워크. 통신 사업자(KT, SKT 등)의 회선을 임대하여 사용하므로 유지비가 비싸고 에러 발생률이 LAN보다 높습니다. |

---

## 2. 이더넷(Ethernet)과 CSMA/CD ★★★
현재 가장 널리 쓰이는 LAN 통신 표준 기술입니다. 

### CSMA/CD (반송파 감지 다중 접속 / 충돌 보존)
이더넷 환경에서 여러 컴퓨터가 **케이블을 공유할 때 데이터 충돌을 막기 위한 통신 규칙**입니다. (시험에서 약자의 의미나 동작 방식을 자주 묻습니다.)

1. **CS (Carrier Sense):** 네트워크 선로를 누가 사용하고 있는지(데이터가 지나가는지) 먼저 엿듣고 감지합니다.
2. **MA (Multiple Access):** 선로가 비어 있으면 누구든지 데이터를 전송할 수 있습니다.
3. **CD (Collision Detection):** 만약 동시에 데이터를 보내어 '충돌'이 발생하면, 즉시 전송을 멈추고 임의의 시간 동안 대기한 후 다시 재전송합니다.

---

## 3. 네트워크 통신 모델 (OSI 7계층 vs TCP/IP)
서로 다른 컴퓨터끼리 통신하기 위해 만든 표준화된 단계입니다.

* **OSI 7계층 (ISO 국제표준화기구 제정):** 이론적인 네트워크 참조 모델입니다. (물리 - 데이터링크 - 네트워크 - 전송 - 세션 - 표현 - 응용)
* **TCP/IP 4계층 (DoD 미 국방성 제정):** 인터넷에서 **실제로 사용되는 실무적인 통신 프로토콜 모델**입니다. (네트워크 인터페이스 - 인터넷 - 전송 - 응용)

> **💡 핵심 기출 포인트: ARP (Address Resolution Protocol)**
> TCP/IP의 인터넷 계층에서 동작하는 매우 중요한 프로토콜입니다.
> **"논리적인 IP 주소를 물리적인 MAC 주소(하드웨어 주소)로 변환해 주는 프로토콜"**을 묻는다면 무조건 정답은 **ARP**입니다. (반대로 MAC을 IP로 바꿔주는 것은 RARP입니다.)

---

## 4. IP 주소 체계 (IPv4 vs IPv6) ★★★
인터넷에 연결된 기기들의 고유한 식별 번호입니다. IPv4의 주소 고갈 문제를 해결하기 위해 IPv6가 등장했습니다. 두 체계의 차이점 비교는 매번 출제됩니다.

| 구분 | IPv4 (Internet Protocol version 4) | IPv6 (Internet Protocol version 6) |
|:---:|---|---|
| **주소 길이** | **32비트** (8비트씩 4부분) | **128비트** (16비트씩 8부분) |
| **표현 방식** | 10진수로 표현하고 **마침표(.)**로 구분<br>(예: `192.168.0.1`) | 16진수로 표현하고 **콜론(:)**으로 구분<br>(예: `2001:0db8:85a3::8a2e...`) |
| **할당 방식** | 유니캐스트, 멀티캐스트, **브로드캐스트** | 유니캐스트, 멀티캐스트, **애니캐스트(Anycast)** |
| **특징** | 현재 가장 많이 쓰이나 주소가 고갈됨 | 보안성(IPsec 기본 제공), 확장성, 자동 설정 기능이 뛰어남 |

---

## 5. 서브네팅 (Subnetting)
주어진 IP 주소 네트워크 대역을 **더 작은 여러 개의 네트워크(서브넷)로 쪼개어 나누는 작업**입니다.

* **목적:** 1. IP 주소의 낭비를 막고 효율적으로 분배하기 위해.
  2. 불필요한 브로드캐스트 트래픽을 줄여 네트워크 성능을 향상시키기 위해.
* **서브넷 마스크 (Subnet Mask):** IP 주소에서 '네트워크 부분'과 '호스트(PC) 부분'을 구분해 주는 역할을 합니다. (예: `255.255.255.0`)
  

# [리눅스 마스터 2급] 3장: 인터넷 활용 - 인터넷 서비스의 종류 출제빈도 상

## 1. 이메일(E-mail) 관련 프로토콜 ★★★
시험에서 가장 많이 묻는 3대장입니다. '보내는 것'과 '받는 것'을 무조건 구분해야 합니다.

| 프로토콜 | 포트 | 역할 | 상세 특징 |
|:---:|:---:|---|---|
| **SMTP** | **25** | 메일 **송신(보내기)** | 클라이언트가 메일 서버로 메일을 보낼 때, 또는 메일 서버끼리 메일을 주고받을 때 사용합니다. |
| **POP3** | **110** | 메일 **수신(받기)** | 서버에 도착한 메일을 내 PC로 **다운로드**합니다. 기본적으로 다운로드 후 서버에서 메일이 삭제됩니다. |
| **IMAP** | **143** | 메일 **수신(받기)** | 서버에 메일을 남겨두고 **동기화**하여 읽습니다. 스마트폰, 태블릿, PC 등 여러 기기에서 메일을 확인할 때 필수적입니다. |

---

## 2. 원격 접속 서비스 (Telnet vs SSH)
다른 컴퓨터에 네트워크를 통해 접속하여 내 컴퓨터처럼 제어하는 서비스입니다.

* **Telnet (포트 23)**
  * 초창기부터 사용되던 원격 접속 서비스입니다.
  * 데이터를 암호화하지 않고 **평문(Plain text)**으로 전송하므로, 중간에 해커가 패킷을 가로채면 비밀번호가 그대로 노출되는 치명적인 보안 취약점이 있습니다.
* **SSH (Secure Shell, 포트 22) ★**
  * Telnet의 보안 문제를 해결하기 위해 등장했습니다.
  * 모든 통신 데이터를 **강력하게 암호화**하여 전송하므로 안전합니다. (현재 실무에서는 Telnet을 버리고 100% SSH만 사용합니다.)

---

## 3. 파일 전송 및 공유 서비스

### FTP (File Transfer Protocol)
* 인터넷을 통해 파일을 송수신하기 위한 서비스입니다.
* **특이점:** 두 개의 포트를 나누어 사용합니다.
  * **포트 21:** 제어(명령어) 연결 (로그인, 디렉터리 이동 등)
  * **포트 20:** 실제 데이터 전송

### NFS (Network File System)
* 썬 마이크로시스템즈(Sun)에서 개발한 프로토콜입니다.
* 네트워크로 연결된 다른 컴퓨터의 하드디스크(디렉터리)를 **마치 내 컴퓨터의 하드디스크처럼 마운트(Mount)**하여 사용할 수 있게 해줍니다.

---

## 4. 웹과 도메인 서비스

### HTTP (HyperText Transfer Protocol, 포트 80)
* WWW(World Wide Web) 환경에서 웹 브라우저와 웹 서버 간에 **HTML 문서(웹 페이지)를 주고받기 위한 통신 규약**입니다. (보안이 강화된 HTTPS는 443번 포트를 사용합니다.)

### DNS (Domain Name System, 포트 53) ★
* `www.google.com` 같은 기억하기 쉬운 **문자형 도메인 주소**를, 컴퓨터가 인식할 수 있는 **숫자형 IP 주소**로 변환해 주는 핵심 서비스입니다.

### `nslookup` (명령어)
* DNS 서버에 직접 질의하여 특정 도메인의 IP 주소를 확인하거나, 반대로 IP 주소로 도메인 네임을 검색할 때 사용하는 네트워크 진단 명령어입니다.

---

## 💡 시험장 가기 전 포트 번호 1초 암기표
| 서비스 | 포트 번호 | 서비스 | 포트 번호 |
|:---:|:---:|:---:|:---:|
| **FTP (데이터)** | 20 | **HTTP (웹)** | 80 |
| **FTP (제어)** | 21 | **POP3 (메일 수신)** | 110 |
| **SSH (보안 원격)** | 22 | **IMAP (메일 동기화)** | 143 |
| **Telnet (일반 원격)**| 23 | **HTTPS (보안 웹)** | 443 |
| **SMTP (메일 송신)** | 25 | | |
| **DNS (도메인)** | 53 | | |


# [리눅스 마스터 2급] 3장: 인터넷 활용 - 네트워크 설정 및 명령어 출제빈도 중

## 1. 네트워크 인터페이스와 주요 설정 파일 ★★★
리눅스 서버에 고정 IP를 할당하거나 DNS 서버를 지정할 때 직접 편집해야 하는 핵심 파일들입니다. (경로 암기 필수)

### `eth0` (Ethernet 0)
* 리눅스 시스템에 장착된 **첫 번째 유선 랜카드(네트워크 인터페이스)**를 지칭하는 기본 이름입니다. 두 번째 랜카드는 `eth1`이 됩니다. (최신 리눅스에서는 `ens33`, `enp0s3` 등으로 이름이 바뀌었으나 시험에서는 주로 `eth0`으로 출제됩니다.)

### `/etc/sysconfig/network-scripts/ifcfg-eth0`
* **RedHat/CentOS 계열의 핵심 네트워크 인터페이스 설정 파일**입니다. IP 주소, 서브넷 마스크, 게이트웨이 등을 설정합니다.
* **시험에 자주 나오는 내부 설정값:**
  * **`BOOTPROTO`**: IP 할당 방식을 결정합니다.
    * `BOOTPROTO=static` (또는 `none`): 고정 IP를 수동으로 입력할 때
    * `BOOTPROTO=dhcp`: IP를 자동으로 할당받을 때
  * **`GATEWAY`**: 외부 네트워크(인터넷)로 나가는 관문 역할을 하는 라우터의 IP 주소를 적습니다.
  * `IPADDR`: IP 주소 / `NETMASK`: 서브넷 마스크

### `/etc/resolv.conf`
* **DNS 서버의 IP 주소를 지정**하는 파일입니다. 웹 서핑 시 도메인 이름을 IP로 변환하기 위해 시스템이 이 파일을 참조합니다.
* *예시 설정:* `nameserver 8.8.8.8` (구글의 공개 DNS 서버 사용)

---

## 2. 네트워크 상태 확인 및 설정 명령어

### `ifconfig` (Interface Configuration)
* 현재 활성화된 네트워크 인터페이스의 상태(IP 주소, MAC 주소, 서브넷 마스크 등)를 확인하거나 설정합니다.
* *예시:* `ifconfig eth0 down` (eth0 랜카드 비활성화) / `ifconfig eth0 up` (활성화)
* (참고: 최신 리눅스에서는 `ip addr` 명령어로 대체되는 추세입니다.)

### `route`
* 시스템의 **라우팅 테이블(경로 설정 표)을 확인하거나 설정**하는 명령어입니다.
* 패킷이 목적지로 가기 위해 어떤 게이트웨이를 거쳐야 하는지 보여줍니다.
* *팁:* `route` 명령어만 치거나 `netstat -nr`을 입력하면 기본 게이트웨이(Default Gateway) 설정 상태를 확인할 수 있습니다.

---

## 3. 네트워크 진단 및 점검 명령어 ★

### `ping`
* 목적지 호스트와 네트워크가 정상적으로 연결되어 있는지 **연결 상태(통신 여부)를 테스트**합니다.
* ICMP 프로토콜을 사용하며, 리눅스에서는 중지(`Ctrl + C`)할 때까지 패킷을 계속 보냅니다. (윈도우는 4번만 보냄)

### `nslookup` (Name Server Lookup)
* 도메인 이름(`www.google.com`)을 입력하여 IP 주소를 얻거나, IP 주소를 입력하여 도메인 이름을 확인(리버스 질의)할 때 사용하는 **DNS 진단 도구**입니다.
* 내부적으로 `/etc/resolv.conf` 파일에 지정된 네임서버를 사용하여 질의합니다.

### `netstat` (Network Statistics)
* 시스템의 **전반적인 네트워크 연결 상태, 라우팅 테이블, 포트 상태**를 보여주는 다목적 명령어입니다. (시험 단골 명령어)
* **자주 쓰이는 주요 옵션 조합:**
  * `-a` : 모든 연결 및 수신 대기(LISTEN) 포트 표시
  * `-n` : 도메인 이름이나 서비스 이름 대신 **숫자(IP 주소, 포트 번호)로 출력** (출력 속도가 빠름)
  * `-t` : TCP 연결만 출력 / `-u` : UDP 연결만 출력
  * `-p` : 해당 포트를 사용 중인 **프로세스 ID(PID)와 프로그램 이름** 출력 (root 권한 필요)
> **기출 예시:** 시스템에 열려 있는 모든 TCP/UDP 포트와 해당 프로그램을 숫자로 보려면? 👉 `netstat -antup`


# [리눅스 마스터 2급] 3장: 리눅스 응용 분야 (기술 동향 및 활용 기술)

## 1. 리눅스 클러스터링 (Clustering) ★★★
여러 대의 컴퓨터를 네트워크로 묶어 마치 '하나의 거대한 컴퓨터'처럼 동작하게 만드는 기술입니다. 시험에서는 목적에 따른 3가지 종류를 무조건 구분해야 합니다.

| 클러스터 종류 | 핵심 목적 | 기출 키워드 및 특징 |
|:---:|---|---|
| **HPC 클러스터**<br>(고성능 컴퓨팅) | **복잡한 과학/수학 연산** | 여러 대의 노드를 묶어 하나의 슈퍼컴퓨터처럼 사용합니다.<br>👉 **베오울프(Beowulf)** 시스템이 가장 대표적입니다. |
| **HA 클러스터**<br>(고가용성) | **24시간 중단 없는 서비스** | 주 서버에 장애(고장)가 발생하면 즉시 예비 서버가 작동을 이어받아 시스템 다운타임을 최소화합니다.<br>👉 백업, 무정지 시스템 |
| **LVS 클러스터**<br>(부하 분산) | **트래픽 집중 방지** | 대규모 웹 서비스 등에서 들어오는 접속 요청(트래픽)을 여러 대의 서버로 골고루 나누어(Load Balancing) 줍니다. |

---

## 2. 클라우드 컴퓨팅 (Cloud Computing) 서비스 모델 ★★★
인터넷을 통해 IT 자원(서버, 스토리지, 소프트웨어 등)을 빌려 쓰는 환경입니다. '무엇을 빌려주는가'에 따라 3가지로 나뉩니다.

* **IaaS (인프라형 서비스)**
  * **서버(CPU, 메모리, 하드디스크)와 네트워크** 같은 물리적 자원(가상 머신)만 빌려줍니다.
  * 사용자가 직접 OS를 설치하고 모든 환경을 세팅해야 합니다. (예: AWS EC2, 카페24 호스팅)
* **PaaS (플랫폼형 서비스)**
  * **개발 환경(OS, 웹 서버, 데이터베이스)**까지 미리 세팅해서 빌려줍니다.
  * 개발자는 서버 관리할 필요 없이 오직 '앱 개발'에만 집중할 수 있습니다. (예: 구글 앱 엔진, AWS Elastic Beanstalk)
* **SaaS (소프트웨어형 서비스)**
  * **완성된 소프트웨어**를 인터넷을 통해 제공합니다.
  * 사용자는 설치할 필요 없이 웹 브라우저로 접속해 바로 사용합니다. (예: 구글 독스, 네이버 마이박스, 드롭박스)

---

## 3. 가상화 (Virtualization) 및 컨테이너
하나의 물리적 서버에서 여러 개의 운영체제나 앱을 동시에 구동하는 기술입니다.

* **하이퍼바이저 (Hypervisor):** 물리적 서버 위에 가상 머신(VM)들을 생성하고 관리하는 중간 계층의 소프트웨어입니다.
  * **Xen (젠):** 반가상화와 전가상화를 모두 지원하는 강력한 하이퍼바이저.
  * **KVM:** 리눅스 커널 자체에 내장된 완전 가상화 기술.
* **컨테이너 (Container):** 무거운 OS 전체를 가상화하지 않고, 프로그램 실행에 필요한 최소한의 라이브러리와 앱만 격리하여 가볍게 실행하는 최신 가상화 방식입니다.
  * 👉 **Docker(도커)**와 **Kubernetes(쿠버네티스)**가 대표적입니다.

---

## 4. 빅데이터(Big Data) 및 임베디드(Embedded) 시스템

### 빅데이터 핵심 프레임워크
* **하둡 (Hadoop) ★:** 대용량의 데이터를 여러 대의 저렴한 컴퓨터(클러스터)에서 병렬로 분산 처리하고 저장할 수 있게 해주는 **대표적인 자바 기반 오픈소스 프레임워크**입니다.
* **NoSQL:** 빅데이터 환경에서 대규모 비정형 데이터를 처리하기 위해 기존의 관계형 데이터베이스(RDBMS)의 한계를 극복한 데이터베이스입니다. (예: MongoDB, Cassandra)

### 임베디드 리눅스 (Embedded Linux)
* 스마트폰, 스마트 TV, 내비게이션, 사물인터넷(IoT) 가전 등 **특정 목적을 가진 소형 기기**에 탑재되도록 가볍게 만든 리눅스입니다.
* **대표적인 OS:**
  * **Android (안드로이드):** 구글이 개발한 스마트폰용 모바일 플랫폼.
  * **Tizen (타이젠):** 삼성전자와 인텔이 주도하여 만든 오픈소스 모바일/스마트 가전용 OS.
  * **GENIVI (제니비):** 차량용 인포테인먼트(IVI) 시스템을 위한 리눅스 표준 플랫폼.